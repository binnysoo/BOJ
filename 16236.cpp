#include <iostream>
#include <queue>
#include <vector>

using namespace std;

int N;
pair<int, int> shark, move, tmp;
vector<vector<int>> map;
bool visited[20][20] = { false, };

int main() {
	int i;
	cin >> N;
}